extends ../layout/layout.jade

include ../components/editable-table.pug

block header
  header
    h1.h1=title
    h2.h2 Administrative Dashboard.
block section
  section#dashboard
    .wrapper
      +editable-table("tutors-table", "Tutors", tutors)
      +editable-table("mentors-table", "Mentors", mentors)
      form(method="get" action="/admin/dashboard/students")
        button.button(type="submit") Download Excel...