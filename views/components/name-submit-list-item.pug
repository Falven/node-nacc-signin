mixin name-submit-list-item(imageSrc, name, description, buttonName)
  li.name-li
    figure
      .portrait
        img(src=imageSrc, alt=name)
      .description
        figcaption.name=name
        p.p.description=description
      button.button.select(name=buttonName value=name type="submit") Select